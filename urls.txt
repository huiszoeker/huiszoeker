SELECT 
  COUNT(*) AS cnt, 
  SUBSTRING(postal_code,0,4) AS four, 
  AVG(price/area) AS average_price 
FROM 
  [funda-195021:funda.sale_utrecht] 
GROUP BY 
  four
ORDER BY 
  average_price DESC


65,80*3+253